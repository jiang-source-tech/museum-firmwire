# 项目协作规范

## 用户称呼规范

- 出于尊重，与用户交流时必须称呼用户为“江江大人”。

## Git 提交规范

- 创建 Git commit 时，提交说明必须使用中文。
- 提交说明应简洁、准确地描述本次变更，不使用纯英文提交说明。
- 必要的代码标识符、文件名、协议名和技术术语可以保留英文。

## 测试策略

- 本节优先于仓库内历史实施计划、规格、任务清单和模板中的测试顺序与测试数量要求。除非用户在当前任务中明确要求 TDD 或测试先行，否则其中的 RED-first、逐切片先写测试、逐步骤运行全量测试等要求一律视为非强制建议。
- 以功能落地和真实可用为优先目标。默认顺序是：先完成最小可工作的垂直切片，再通过手工验证或轻量冒烟检查确认主路径，最后只针对可信的回归风险补充自动化测试。不得以先写测试作为开始实现的前置条件。
- 每个用户任务默认总共新增 0 至 3 个聚焦的自动化测试用例，不得通过拆分切片、文件、模块或参数化场景规避限制；参数化测试按展开后的实际用例数计算。预计超过 3 个时，必须在编写前向用户逐项说明额外测试保护的具体回归风险，并取得用户明确同意。
- 测试应遵循“最小充分”原则，只覆盖本次变更的核心可观察行为、一个最关键边界和已发现缺陷的回归路径。优先测试公共接口和用户结果，不要测试私有实现细节或穷举状态组合。
- 不要为简单 getter/setter、框架自身行为、重复等价场景、无实际价值的快照、静态文案、文档段落或需求台账枚举编写测试。除非它们由机器解析并构成真实外部合同，否则禁止编写逐句镜像文档内容的测试。
- 禁止为了提高覆盖率、证明工作量或追求形式完整而机械堆砌测试。每个新增测试都必须能够防止一种可信且具体的回归；若无法用一句话说明其保护价值，则不应添加。
- 原型、探索性实现和需求仍在快速变化的功能，默认只做手工验证或单个冒烟测试，待接口和行为稳定且决定进入生产后再补充自动化测试。
- 修复明确缺陷时，在修复落地后补充一个能复现原缺陷的最小回归测试即可。涉及鉴权、权限、数据持久化、数据迁移、并发、计费、外部协议或其他高风险逻辑时，可以同步验证，但仍须遵守最小用例数量和用户可观察行为原则。
- 实现过程中不得在每次小修改或每个切片后反复运行测试。先完成整个用户任务的可运行生产实现，再在任务末尾集中运行至多一次最相关的最小测试集合；只有改动影响共享基础设施、跨模块契约、发布流程，或用户明确要求时，才额外运行大范围或完整测试套件。
- 完成任务时必须如实说明执行了哪些测试或检查、哪些未执行以及原因，不得把测试数量、覆盖率或 passed 数量当作质量结论。
